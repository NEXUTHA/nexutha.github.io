<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="NEXUTHAはAIを活用したビジネスソリューションを提供。データ分析、システム統合、リアルタイムインサイトであなたのビジネスを変革します。">
  <title>NEXUTHA - AIでビジネスを変革</title>
  <style>
    :root {
      --primary-color: #3f61ff;
      --bg-dark: rgba(0, 0, 0, 0.6);
      --text-color: #fff;
      --shadow-color: rgba(0, 0, 0, 0.3);
    }

    html, body {
      margin: 0;
      padding: 0;
      background: #0d0d0d url('https://images.unsplash.com/photo-1549887534-217c57f4658d?auto=format&fit=crop&w=1200&q=80') no-repeat center center fixed;
      background-size: cover;
      color: var(--text-color);
      font-family: "Helvetica Neue", sans-serif;
      overflow-x: hidden;
    }

    section, header, footer {
      padding: 60px 20px;
      max-width: 1000px;
      margin: auto;
      animation: fadeIn 1.2s ease forwards;
      opacity: 0;
      background: var(--bg-dark);
      border-radius: 10px;
    }

    header { text-align: center; }
    h1 { font-size: 2.5rem; margin-bottom: 10px; }
    @media (max-width: 600px) {
      h1 { font-size: 1.8rem; Cun}
      section, header, footer { padding: 40px 15px; }
    }

    .btn {
      display: inline-block;
      padding: 12px 24px;
      margin: 10px;
      background: var(--primary-color);
      color: var(--text-color);
      border-radius: 5px;
      text-decoration: none;
      transition: background 0.3s ease;
    }
    .btn:hover { background: #2a4bff; }

    .logos {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 20px;
      opacity: 0.8;
    }
    .logos img { max-width: 100px; height: auto; }

    .features {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;
    }
    .feature {
      flex: 1 1 280px;
      background: #1a1a1a;
      padding: 20px;
      border-radius: 10px;
      transition: transform 0.3s ease;
    }
    .feature:hover { transform: translateY(-5px); }

    .blue-section {
      background: var(--bg-dark);
      text-align: center;
      padding: 100px 20px;
      color: var(--text-color);
    }
    @media (max-width: 600px) {
      .blue-section { padding: 60px 15px; }
    }

    .faq h3 {
      cursor: pointer;
      margin: 10px 0;
      transition: color 0.3s ease;
    }
    .faq h3:hover { color: var(--primary-color); }
    .faq p {
      display: none;
      margin-bottom: 20px;
      transition: max-height 0.3s ease, opacity 0.3s ease;
      max-height: 0;
      opacity: 0;
      overflow: hidden;
    }
    .faq p.show {
      display: block;
      max-height: 200px;
      opacity: 1;
    }

    footer {
      font-size: 0.9em;
      text-align: center;
      border-top: 1px solid #333;
      margin-top: 40px;
    }

    canvas {
      position: fixed;
      top: 0;
      left: 0;
      pointer-events: none;
      z-index: 9999;
    }

    .lang-switch {
      position: absolute;
      top: 20px;
      right: 20px;
      z-index: 9999;
    }

    .x-button {
      position: fixed;
      top: 20px;
      left: 20px;
      background-color: #1da1f2;
      color: var(--text-color);
      padding: 10px 16px;
      border-radius: 30px;
      text-decoration: none;
      font-weight: bold;
      z-index: 9999;
      box-shadow: 0 4px 12px var(--shadow-color);
      transition: background 0.3s ease;
    }
    .x-button:hover { background-color: #0c85d0; }

    @keyframes fadeIn { to { opacity: 1; } }
  </style>
</head>
<body>
  <!-- X フォロー -->
  <a class="x-button" href="https://x.com/nexutha1?s=21" target="_blank" aria-label="NEXUTHAをXでフォロー">X フォロー</a>

  <!-- 言語切替 -->
  <div class="lang-switch">
    <a class="btn" href="index_en.html" aria-label="英語ページに切り替え">ENGLISH</a>
  </div>

  <!-- 背景Canvas -->
  <canvas id="orbCanvas" aria-hidden="true"></canvas>

  <header>
    <h1>ビジネスを変革するAIの力</h1>
    <p>データで可能性を拡張するAI時代のスタート</p>
    <a href="music.html" class="btn" aria-label="音楽リリースページへ">音楽リリースを見る</a>
  </header>

  <section aria-labelledby="partners">
    <h2 id="partners" class="visually-hidden">パートナーロゴ</h2>
    <div class="logos">
      <img src="https://via.placeholder.com/100x40?text=ロゴ1" alt="パートナー1のロゴ" loading="lazy">
      <img src="https://via.placeholder.com/100x40?text=ロゴ2" alt="パートナー2のロゴ" loading="lazy">
      <img src="https://via.placeholder.com/100x40?text=ロゴ3" alt="パートナー3のロゴ" loading="lazy">
      <img src="https://via.placeholder.com/100x40?text=ロゴ4" alt="パートナー4のロゴ" loading="lazy">
    </div>
  </section>

  <section aria-labelledby="features">
    <h2 id="features" class="visually-hidden">機能紹介</h2>
    <div class="features">
      <div class="feature">
        <h3>AI予測</h3>
        <p>最適な予測でビジネスを支援します。</p>
      </div>
      <div class="feature">
        <h3>連携と統合</h3>
        <p>既存のシステムとスムーズに統合可能。</p>
      </div>
      <div class="feature">
        <h3>即時インサイト</h3>
        <p>リアルタイムにAIが意思決定を支援。</p>
      </div>
    </div>
  </section>

  <section class="blue-section" aria-labelledby="experience">
    <h2 id="experience">NEXUTHAの体験</h2>
    <p>業界を越えたAIソリューションをあなたに</p>
  </section>

  <section aria-labelledby="faq">
    <h2 id="faq">よくある質問</h2>
    <div class="faq">
      <h3 tabindex="0" role="button" aria-expanded="false" onclick="toggleFAQ(this)" onkeydown="handleKeydown(event, this)">AIはどう動作しますか？</h3>
      <p>機械学習で予測と分析を行います。</p>
      <h3 tabindex="0" role="button" aria-expanded="false" onclick="toggleFAQ(this)" onkeydown="handleKeydown(event, this)">他のツールと連携できますか？</h3>
      <p>API接続で柔軟に対応可能です。</p>
      <h3 tabindex="0" role="button" aria-expanded="false" onclick="toggleFAQ(this)" onkeydown="handleKeydown(event, this)">データは安全ですか？</h3>
      <p>エンタープライズ水準の暗号化を施しています。</p>
    </div>
  </section>

  <footer>
    <p>© 2025 NEXUTHA. All rights reserved. | <a href="/privacy">プライバシーポリシー</a> | <a href="/terms">利用規約</a></p>
  </footer>

  <script>
    // FAQトグル機能
    function toggleFAQ(el) {
      const p = el.nextElementSibling;
      const isExpanded = el.getAttribute('aria-expanded') === 'true';
      el.setAttribute('aria-expanded', !isExpanded);
      p.classList.toggle('show');
    }

    // キーボード操作対応
    function handleKeydown(event, el) {
      if (event.key === 'Enter' || event.key === ' ') {
        event.preventDefault();
        toggleFAQ(el);
      }
    }

    // キャンバスアニメーション
    const canvas = document.getElementById('orbCanvas');
    const ctx = canvas.getContext('2d');

    function resizeCanvas() {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    }
    resizeCanvas();
    window.addEventListener('resize', resizeCanvas);

    const orbs = [
      { x: 0, y: 0, hue: 0, speed: 3 },
      { x: 100, y: 100, hue: 120, speed: 2 },
      { x: 200, y: 200, hue: 240, speed: 4 }
    ];

    document.addEventListener('mousemove', e => {
      orbs[0].x = e.clientX;
      orbs[0].y = e.clientY;
    });

    function draw() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      orbs.forEach(orb => {
        orb.hue = (orb.hue + orb.speed) % 360;
        ctx.beginPath();
        ctx.arc(orb.x, orb.y, 20, 0, Math.PI * 2);
        ctx.fillStyle = `hsl(${orb.hue}, 100%, 60%)`;
        ctx.shadowColor = ctx.fillStyle;
        ctx.shadowBlur = 20;
        ctx.fill();
      });
      requestAnimationFrame(draw);
    }
    draw();
  </script>
</body>
</html>
