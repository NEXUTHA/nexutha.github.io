// NEXUTHA - Main TypeScript Entry Point
import { nexuthaApp } from './app';
import { chatbotUI } from './chatbot-ui';

// The app is already initialized in app.ts through the module system
// Just log that everything is ready
console.log('🚀 NEXUTHA - All systems operational!');